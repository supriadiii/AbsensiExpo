import React from "react";
import { View, Text } from "react-native";

const Home = (props: any) => {
  console.log({ props });
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
